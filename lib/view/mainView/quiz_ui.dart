import 'package:flutter/material.dart';

class QuizUi extends StatelessWidget {
  const QuizUi({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(child: Text("quizUI"));
  }
}
